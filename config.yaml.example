# Example configuration for task CLI
# Copy to ~/.config/task-cli/config.yaml

# Task storage settings
storage:
  format: json
  max_tasks: 1000

# Display settings
display:
  color: true
  unicode: true
